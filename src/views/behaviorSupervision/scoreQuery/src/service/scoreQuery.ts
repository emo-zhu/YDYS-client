import { reactive } from 'vue'
import { StepModalFactory } from 'junegoo-ui'

export const supplementModal = reactive(new StepModalFactory())
export const viewModal = reactive(new StepModalFactory())
